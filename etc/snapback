# Mountpoint of snapshots
SNAPDIR="/.snapshots"

# Keep last n snapshots available (for fast recovery, etc.)
KEEP=30

# Backup options for borg
BORGREPO="borg@example.com:hostname"
BORGARGS="-v --show-rc --stats --compression zlib,9"

