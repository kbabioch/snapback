- DEBUG conditionally (debug flag in config?)

- logging (to stdout?)

- make aspects configurable (/conf/snapback), e.g. subvolumes, repo, etc.

- Make use of shellcheck

- Make use of shunit2

- Hook for pacman?

- Rename snapback to snapbackctl?

